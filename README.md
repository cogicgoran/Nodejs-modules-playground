# Nodejs native modules playground

## Football team builder

Build imaginary football teams

Trying to understand native modules provided by nodejs.

* Run `npm start` to run application.
* Run `npm run test` to run tests.
